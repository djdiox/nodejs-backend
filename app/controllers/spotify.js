/**
 * The handler for Spotify Route access.
 */
module.exports = {
    handlers: () => {
        // credentials are optional
        const spotifyCallback = (req, res, next) => {

        };
        return {
            spotifyCallback
        };
    }
};